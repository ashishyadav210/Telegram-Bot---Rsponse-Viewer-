# Telegram Response Viewer Bot (PHP)

यह एक आसान और शक्तिशाली टेलीग्राम बॉट स्क्रिप्ट है जो PHP में लिखी गई है। इसका मुख्य काम गेमिंग गिल्ड (जैसे Free Fire) के लिए नए सदस्यों का रजिस्ट्रेशन करना है। यह बॉट यूज़र्स से स्टेप-बाय-स्टेप जानकारी लेता है और उसे एडमिन की टेलीग्राम आईडी पर भेज देता है।

This is a simple yet powerful Telegram bot script written in PHP. Its primary function is to handle new member registrations for a gaming guild (e.g., Free Fire). The bot collects information from users step-by-step and forwards it directly to the admin's Telegram ID.

## ✨ मुख्य विशेषताएँ (Features)

*   **स्टेप-बाय-स्टेप रजिस्ट्रेशन (Multi-Step Form):** यूज़र्स से बातचीत के जरिए स्टेप्स में जानकारी लेता है (जैसे FF UID, Username, Rank आदि)।
*   **एडमिन नोटिफिकेशन (Admin Notifications):** कोई भी नया रजिस्ट्रेशन पूरा होने पर, एडमिन को तुरंत यूज़र की पूरी जानकारी के साथ एक मैसेज मिलता है।
*   **एडमिन पैनल (Admin Panel):** एक वेब-आधारित एडमिन डैशबोर्ड जहाँ आप बॉट के आँकड़े (Stats) देख सकते हैं, जैसे कुल यूज़र्स, एक्टिव रजिस्ट्रेशन और हाल की एक्टिविटी।
*   **कोई डेटाबेस नहीं (No Database Required):** यह बॉट यूज़र का डेटा और लॉग्स सर्वर पर ही `.json` फ़ाइलों में स्टोर करता है, इसलिए आपको MySQL जैसे किसी बाहरी डेटाबेस की ज़रूरत नहीं है।
*   **आसान सेटअप (Easy Setup):** बॉट को कॉन्फ़िगर करना और सर्वर पर चलाना बहुत आसान है।
*   **कस्टमाइज़ करने योग्य (Customizable):** आप अपनी ज़रूरत के हिसाब से रजिस्ट्रेशन के सवालों को आसानी से बदल सकते हैं।

## ⚙️ इंस्टॉलेशन (Installation & Setup)

बॉट को सेट करने के लिए नीचे दिए गए स्टेप्स को फॉलो करें:

1.  **फ़ाइलें डाउनलोड करें (Download Files):**
    इस रिपॉजिटरी की सभी फ़ाइलों को डाउनलोड करें और अपने वेब होस्टिंग (cPanel या किसी अन्य) पर अपलोड करें जहाँ PHP चलता हो।

2.  **कॉन्फ़िगरेशन एडिट करें (Edit Configuration):**
    `config.php` फ़ाइल को एडिट करें और अपनी जानकारी भरें:
    *   `YOUR_BOT_TOKEN_HERE`: यहाँ अपना टेलीग्राम बॉट टोकन डालें जो आपको BotFather से मिलेगा।
    *   `YOUR_ADMIN_TELEGRAM_ID_HERE`: यहाँ अपनी टेलीग्राम यूज़र आईडी डालें, जिस पर आप रजिस्ट्रेशन की जानकारी पाना चाहते हैं।

3.  **वेबहुक सेट करें (Set Webhook):**
    अपने ब्राउज़र में `setup.php` फ़ाइल को एक बार खोलें। उदाहरण: `https://yourdomain.com/path/to/files/setup.php`
    यह स्क्रिप्ट आपके बॉट के लिए वेबहुक अपने आप सेट कर देगी।
    *   **सुरक्षा चेतावनी:** सेटअप पूरा होने के बाद, सुरक्षा कारणों से `setup.php` फ़ाइल को अपने सर्वर से **हटा दें (delete)** या उसका नाम बदल दें।

4.  **एडमिन पासवर्ड बदलें (Change Admin Password):**
    `admin.php` फ़ाइल खोलें और सुरक्षा के लिए डिफ़ॉल्ट पासवर्ड `Naresh` को बदल दें।

आपका बॉट अब इस्तेमाल के लिए तैयार है!

## ✏️ सवालों को कैसे बदलें (How to Customize Questions)

आप यूज़र्स से पूछे जाने वाले सवालों को अपनी सुविधा के अनुसार आसानी से बदल सकते हैं। सभी सवाल `index.php` फ़ाइल में मौजूद हैं।

उदाहरण के लिए, अगर आप "FF UID" की जगह "Game ID" पूछना चाहते हैं:

*   `index.php` फ़ाइल खोलें।
*   `startGuildForm` फ़ंक्शन में `sendMessage($chatId, 'Please enter your FF UID...');` लाइन को खोजें।
*   इस लाइन में `'Please enter your FF UID...'` टेक्स्ट को अपनी पसंद के सवाल से बदल दें, जैसे `'Please enter your Game ID...'`।

इसी तरह, आप `handleFFUIDStep`, `handleBermudaRankStep` जैसे सभी फंक्शन्स में जाकर सवालों के टेक्स्ट को बदल सकते हैं।

## 📂 फ़ाइल स्ट्रक्चर (Project Files)

*   `index.php`: बॉट का मुख्य लॉजिक, जो मैसेज और कमांड को हैंडल करता है।
*   `admin.php`: वेब-आधारित एडमिन पैनल।
*   `config.php`: बॉट टोकन और एडमिन आईडी जैसी सभी कॉन्फ़िगरेशन।
*   `setup.php`: वेबहुक सेट करने के लिए स्क्रिप्ट।
*   `database.php`: फ़ाइल-आधारित डेटा को मैनेज करने के लिए क्लास।
*   `/data/` & `/logs/`: ये डायरेक्टरी यूज़र डेटा और लॉग्स को स्टोर करने के लिए अपने आप बन जाती हैं।
*   
